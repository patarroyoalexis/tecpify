NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# opcional: aislada del runtime normal; solo la lee lib/supabase/internal/service-role-client.ts
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# tests E2E con Playwright
# opcional: si no se define, Playwright intenta registrar usuarios unicos via /api/auth/register
PLAYWRIGHT_BASE_URL=http://localhost:3000
E2E_TEST_EMAIL_DOMAIN=
PLAYWRIGHT_OWNER_EMAIL=
PLAYWRIGHT_OWNER_PASSWORD=
PLAYWRIGHT_INTRUDER_EMAIL=
PLAYWRIGHT_INTRUDER_PASSWORD=
# opcional: usa 1 para correr contra un servidor ya levantado manualmente
PLAYWRIGHT_SKIP_WEBSERVER=
# opcional: lo usan runners automatizados para activar retries/reportes de CI
CI=
